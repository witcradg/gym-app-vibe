# gym-app-vibe

Minimal Next.js base app scaffolded with App Router and TypeScript.

## Run

```bash
npm install
npm run dev
```

## Build Metadata

Build and release metadata used by the `/about` page is documented in [docs/build-metadata.md](docs/build-metadata.md).
